- 计算功能完全一致，界面基本一致。

EXE 相对 HTML 的更改：

1. 所有配置（包括全局设置，武器库，圣遗物库）会自动保存到同目录的 date 文件夹下。
2. 打开时，会进入配置管理界面，可以选择目前 date 文件夹中已有的配置进入，也可以新建配置。
3. 进入配置后即为和 HTML 相同的编辑界面，但相对 HTML 更改：
    - 在中间的上方新增一个类似灵动岛的显示框，显示当前的配置名，并且点击显示框后文字会变为选择`保存并退出`，`不保存并退出`，`快速拉表`，`设置`四个按钮图标。
    - 删除右上角已有的四个按钮，公式按钮移到配置管理界面。
    - 显示框会自动收缩到屏幕上方，鼠标移到上方时再弹出。