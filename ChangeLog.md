# 更新日志

本文件记录对本仓库的所有改动。版本号与计算器主版本保持一致。

---

## v2.1.9 · EXE 构建 2（2026-09-24）

本次只动 EXE（`exe_src/`），目标是把桌面端的**动画、界面与部分交互逻辑**对齐
`html_bin/Gi DMG v2.1.9.html`（除 `user_upload/What_Change.md` 里已列出的差异外）。
HTML 版本文件未作改动——本次改的正是「EXE 与网页不一致」的地方，网页侧没有对应修改点。
计算引擎与对拍结果未受影响（`exe_src/tests` 27 个用例全部通过）。

### 新增：动效工具箱 `exe_src/gidmg/ui/motion.py`

网页的观感大量依赖 CSS `transition`，Qt 样式表的 `:hover` 却是硬切换，所以新增一层动效工具，
让自绘控件按进度插值颜色 / 几何，复刻网页的过渡时长（.12s ~ .2s，`ease`）：

- `Tween` / `HoverTracker`：任意控件的「悬停 / 按下」0~1 进度，`paintEvent` 里插值即可。
- `focus_glow()`：模拟 `.pi:focus` 的 `box-shadow: 0 0 0 3px rgba(11,87,208,.12)` 外发光。
- `fade_in()` / `window_fade_in()`：控件与窗口的淡入（窗口另带 8px 上浮）。
- `Scrim`：主窗口上的弹窗遮罩，颜色取网页的 `rgba(24,34,51,.38)`，带淡入淡出与嵌套计数。
- `SmoothScroll`：滚轮平滑滚动，接近浏览器的滚动手感。
- `StackFader`：工作区换页时的淡入。

### 界面：按网页最终层叠结果重绘控件

- **按钮**：新增自绘的 `widgets.MotionButton`，背景 / 描边 / 文字 / 图标颜色都按 hover、按下、
  选中进度插值。各类按钮的配色与尺寸逐条对齐网页的 `.bg`（主按钮：悬停加深 + 上浮 1px + 蓝色投影）、
  `.bs`（次按钮：透明底描边胶囊）、`.bgh`、`.chip`（选中态取 `.contrib-chip.is-selected` 的
  `#dbe8ff / #8ab0f0 / #174ea6`）、`.sidebar-entry`、`.sidebar-icon-button`、`.sidebar-add-button`。
- **左侧角色行**：改为 34px 胶囊（网页 `.sidebar-character-row` 的最终形态）——元素图标去掉圆底直接摆放、
  名称与状态同一行、开关不再缩放；选中高亮改为渐变过渡；在「魔物设置 / 时间轴」工作区里会收起选中高亮
  （与网页 `#genshinView[data-workspace]` 的规则一致）。
- **下拉框**：`widgets.Select` 自绘 `.studio-select` 外观——悬停浅灰、展开时白底蓝边 + 光晕、
  右侧箭头 45° → 225° 旋转，弹出面板淡入下滑；占位文案用网页的 `#8a94a2`。
- **输入框**：字号与网页 `.pi` 一致（11.5px / 32px / 10px 圆角），聚焦时补上蓝色外发光。
- **开关**：`.tgl` 的 36×20 轨道 + 14px 滑块，轨道色与滑块位移分别按 .2s 过渡，悬停变深。
- **滑块**：新增 `widgets.Slider`，4px 轨道 + 16px 白底蓝边滑块，悬停外扩 6px 光环、按下放大 1.18 倍
  （网页 `input[type=range]` 的写法），点哪跳哪。
- **循环时间轴**：整块重绘为网页的最终形态——18px 圆角板面、`150px + 1fr` 栅格、38px 刻度行
  （只留 6px 短刻度、不再画数字）、26px 轨道 + 4px 间距 + 2px 中线 + 每刻度一条竖网格；
  区间改为「两端 12px 白底描边圆帽 + 中间 4px 色线」（随动模式中段为白底内描边），瞬发改为 16px 空心圆环；
  角色行悬停有 `#fafbfd` 的背景过渡。统计卡与「轴长设置」按钮也按 `.timeline-stat` / `.timeline-settings-btn` 调整。
- **弹窗**：`dialogs/base.Modal` 改为无边框 24px 圆角卡片 + 浮动投影，出现时主窗口盖一层遮罩、
  窗体淡入上浮，并像网页那样自动把焦点落到第一个输入控件；标题栏可拖动。
- **右侧统计栏**：伤害统计列表改为独立滚动、最高 410px（网页 `#resList` 的 `max-height: min(46vh,410px)`），
  总期望与 DPS 始终留在视野里。
- **其它**：结果卡片、配置卡片、历史记录行改为渐变悬停；伤害占比环形图的扇区悬停淡出到 .85；
  菜单圆角与悬停色对齐 `#addDmgMenu`；滚动条 8px → 6px；左侧「角色」标题按钮按
  `.sidebar-section-title-button` 收到 32px、选中时加粗；灵动岛按钮悬停渐变、展开时两页交叉淡入。

### 逻辑

- **补上「重置配置」**：网页在右上角「更多」菜单里，EXE 之前没有入口；现放在全局设置弹窗底部，
  行为与网页一致（清空所有角色与基准值，二次确认文案同为「清空所有配置？」）。
- **快捷键与文档对齐**：`Ctrl+Q` 之前被绑成「快速拉表」，与 README 的说明矛盾；现改为
  `Ctrl+Q` = 保存并退出，新增 `Ctrl+T` = 快速拉表，README 同步更新。
- `tools/dev_preview.py` 截图前会等动画跑完，避免拍到补间的中间帧。

### 文档

- `README.md`：补充 `Ctrl+T` 快捷键、说明「重置配置」在 EXE 里的位置，并更新界面截图。

---

## v2.1.9 · EXE 构建 1（2026-09-24）

### 新增：原生 EXE 版本（完全重写，不再套壳 HTML）

以 **Python 3 + PySide6（Qt Widgets + QSS）** 重写了桌面端，源码位于 `exe_src/`。
桌面端没有内嵌任何浏览器内核，界面由 Qt 原生控件绘制，计算引擎是独立的 Python 实现。

**计算层（`exe_src/gidmg/core/`）**

- `constants.py`：元素、伤害类型、反应倍率、等级系数、效果分类、快捷配置助手用的各类数值表。
- `state.py`：配置结构、默认值、归一化与迁移、各类校验（伤害来源是否完整、效果是否有效等）。
- `engine.py`：伤害结算主引擎——普通伤害 / 增幅反应 / 剧变反应 / 特殊直伤 / 特殊反应多角色加权、
  羽毛附加伤害、元素共鸣、防御区、抗性区、循环时间轴 20 段均匀采样。
- `quickset.py`：快捷配置助手（突破推算、武器白值与副词条推算、圣遗物主副词条、套装与武器天赋）。
- `quicktable.py`：快速拉表的组合枚举、偏序剔除、排序与 CSV / Markdown 导出。
- `storage.py`：`date/` 目录的读写（配置、草稿、武器库、圣遗物库、历史记录、全局设置），原子写入。
- `format.py`：与网页版一致的数字格式化（千分位、DPS、百分比、时间）。

**界面层（`exe_src/gidmg/ui/`）**

- 配置管理界面：列出 `date/configs/` 下的配置，支持打开、重命名、复制、删除、导入 / 导出、示例模板新建；
  「计算公式」入口移到此页。
- 编辑界面：左侧导航（魔物设置 / 角色列表 / 元素共鸣 / 历史记录 / 时间轴）、中间工作区、右侧伤害统计与基准对比，
  与网页版布局一致。
- 灵动岛：顶部中央显示当前配置名，点击展开为 保存并退出 / 不保存并退出 / 快速拉表 / 设置 四个按钮，
  平时缩在窗口顶边，鼠标靠近自动弹出。
- 弹窗：全局设置、历史记录、快捷配置助手（含武器库 / 圣遗物库）、快速拉表（收集方案 → 后台计算 → 结果表）、
  面板明细、伤害占比（环形图）、计算公式总览、伤害来源面板时序解析。
- 图标与元素图形从 HTML 版内置资源导出，保持视觉一致；图标授权声明见
  `exe_src/gidmg/assets/icons/NOTICE.md`。

**与 HTML 版的行为差异**（按 `user_upload/What_Change.md` 实现）

1. 全局设置、武器库、圣遗物库、各份配置自动保存到 EXE 同目录的 `date/` 文件夹。
2. 启动后先进入配置管理界面，可选择已有配置或新建。
3. 进入配置后顶部中央是灵动岛，取代网页版右上角的四个按钮；公式按钮移至配置管理界面。
4. 配置编辑在「保存并退出」时落盘，编辑过程中定期写草稿，异常退出后可恢复。

**测试**

- `exe_src/tests/`：把 `html_bin/Gi DMG v2.1.9.html` 里的网页脚本抽出来在 Node 上执行，
  与 Python 引擎逐字段对拍（总伤害、DPS、每个伤害来源的期望 / 暴击 / 非暴击、角色面板、伤害占比、
  快捷配置助手推算结果），另有存档与拉表的单元测试，共 22 个用例。
- 没装 Node 时对拍用例自动跳过，其余用例照常运行。

**构建**

- `exe_src/build.py` + `build.bat`（Windows 一键）/ `build.sh`（本地自测）：装依赖 → 跑测试 → PyInstaller 单文件打包
  → 产物复制到 `exe_bin/Gi DMG v2.1.9.exe`。
- `.github/workflows/build-exe.yml`：在 `windows-latest` 上执行同一套流程，产物作为 Artifact 上传；
  手动触发勾选 `commit_binary`，或提交信息里带 `[commit-exe]`，会把 EXE 提交回 `exe_bin/`。

### 其他

- 补齐仓库根目录的 `README.md`（功能、差异、数据目录、源码结构、构建与测试说明）。
- 新增 `.gitignore`（虚拟环境、PyInstaller 中间产物、运行时 `date/` 目录等）。
- `html_bin/Gi DMG v2.1.9.html`：从 `user_upload/` 同步一份到成果目录，内容与原文件一致；
  网页版的功能与公式本次没有改动，因此 HTML 侧无需重新构建。
